<core:FragmentDefinition
		xmlns="sap.m"
		xmlns:f="sap.ui.layout.form"
		xmlns:core="sap.ui.core">
	<ResponsivePopover
			id="detailsPopover"
			title="Equipment : {title}"
			class="sapUiResponsivePadding--header"
			placement="Auto">
		<f:SimpleForm
				id="appointmentEditForm"
				editable="false"
				layout="ResponsiveGridLayout"
				singleContainerFullSize="false">
			<!-- <Label text="Maint Item"/>
			<Text text="{Wapos}"/> -->
			<Label text="Maint Plan"/>
			<Text text="{Warpl}"/>
			<Label text="Last Order Number"/>
			<Text text="{Laufn}"/>
		</f:SimpleForm>
	</ResponsivePopover>
</core:FragmentDefinition>